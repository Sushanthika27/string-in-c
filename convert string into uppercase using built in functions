#include<stdio.h>
#include<string.h>
int main()
{
   char a[30];
   int i=0;
   printf("\nEnter a string");
   fgets(a,sizeof(a),stdin);
   while(a[i]!='\0')
   {
      a[i]=toupper(a[i]);
      i++;
   }
   puts(a);
   return 0;
}
