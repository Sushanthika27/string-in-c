#include<stdio.h>
#include<string.h>
#                                             include<ctype.h>
int main()
{
   char str1[100],str2[100],str3[100],upper,lower,reverse;
   printf("\nEnter a string 1:");
   fgets(str1,sizeof(str1),stdin);
   printf("\nEnter the string 2:");
   fgets(str2,sizeof(str2),stdin);
   printf("\nlength of the string 1=%d",strlen(str1));
   printf("\nlength of the string 2=%d",strlen(str2));
   printf("\nAfter copying str1 to str3:%s",str3);
   strcat(str1,str2);
   printf("\nAfter concatenation:%s",str1);
   if((strcmp(str1,str2)==0))
   {
      printf("\n Both the strings are equal");
   }
   else if(strcmp(str1,str2)>0)
   {
      printf("\n string 1 is greater");
   }
   else
   {
      printf("\n string 2 is greater");
   }
   printf("\nreversed first string:%s",strrev(str1));
   printf("\nLower case of first string :%s",strlwr(str1));
   printf("\nUpper case of second string is:%s",strupr(str2));
   return 0;
}
