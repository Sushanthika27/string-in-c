#include<stdio.h>
#include<string.h>
int main()
{
   char str[100],res;
   int i=0;
   printf("\nEnter the string");
   fgets(str,sizeof(str),stdin);
   while(str[i]!='\0')
   {
      if(str[i]>='a'&&str[i]<='z')
      {
         str[i]=str[i]-32;
      }
      else
      {
         str[i]=str[i];
      }
      i++;
   }
   puts(str);
   return 0;
}
